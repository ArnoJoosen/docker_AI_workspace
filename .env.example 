# --------------------------------------------------------------------------
# Docker Build Configuration
# --------------------------------------------------------------------------

# Target build environment. Options: 'cuda', 'rocm', 'cpu'
BUILD_TARGET=cpu

# Python version to install in the container (e.g., 3.10, 3.11)
PYTHON_VERSION=3.10

# --------------------------------------------------------------------------
# User and Permissions Configuration
# --------------------------------------------------------------------------

# The username for the non-root user inside the container.
DOCKER_USERNAME=user

# Set the user and group ID to match your host user.
# On Linux/macOS, you can find these by running `id -u` and `id -g` in your terminal.
USER_UID=1000
USER_GID=1000

# --------------------------------------------------------------------------
# Security and Access Configuration
# --------------------------------------------------------------------------

# Your public SSH key. This will be added to the container's authorized_keys.
# Example: SSH_PUBLIC_KEY="ssh-rsa AAAA..."
SSH_PUBLIC_KEY=

# A secure token for accessing JupyterLab.
JUPYTER_TOKEN=my_secure_jupyter_token_123

# Password for the non-root user.
USER_PASSWORD=user

# Password for the root user.
ROOT_PASSWORD=root
